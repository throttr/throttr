name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        size: [UINT8, UINT16, UINT32, UINT64]

    container:
      image: ghcr.io/throttr/builder-alpine:1.87.0-debug

    steps:
      - uses: actions/checkout@v4
      - name: Build and test
        run: |
          mkdir -p build
          cd build
          cmake .. -DCMAKE_BUILD_TYPE=Debug -DBUILD_TESTS=ON -DRUNTIME_VALUE_SIZE=${{ matrix.size }}
          make -j4
          ctest  --output-on-failure --verbose
          cd ..
          gcovr --filter='src/' --print-summary --sort-percentage

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}